{
  "grids": [
    {
      "id": "grid_dfs_basic",
      "name": "DFS on a Grid — Explore Region",
      "category": "grids",
      "topic": "dfs",
      "weight": 10,
      "description": "Perform depth-first search on a 2D grid to explore a connected region of cells that match a target value. Handle boundaries and visited tracking.",
      "signature": "def grid_dfs(grid: List[List[int]], r: int, c: int, target: int) -> int:"
    },
    {
      "id": "grid_bfs_basic",
      "name": "BFS on a Grid — Explore Region",
      "category": "grids",
      "topic": "bfs",
      "weight": 10,
      "description": "Perform breadth-first search from a starting position in a 2D grid to traverse all connected cells with the same value. Return the size of the region.",
      "signature": "def grid_bfs(grid: List[List[int]], r: int, c: int) -> int:"
    },
    {
      "id": "grid_multi_source_bfs",
      "name": "Multi-Source BFS on Grid",
      "category": "grids",
      "topic": "bfs",
      "weight": 9,
      "description": "Run BFS starting from multiple initial grid cells simultaneously to compute the minimum distance/time to fill or reach each cell.",
      "signature": "def multi_source_bfs(grid: List[List[int]], sources: List[Tuple[int,int]]) -> int:"
    },
    {
      "id": "grid_count_components",
      "name": "Count Connected Components in Grid",
      "category": "grids",
      "topic": "dfs",
      "weight": 10,
      "description": "Given a 2D grid, count how many connected components exist where cells match a given value such as 1s forming islands.",
      "signature": "def count_components(grid: List[List[int]]) -> int:"
    },
    {
      "id": "grid_shortest_path",
      "name": "Shortest Path in a Grid",
      "category": "grids",
      "topic": "bfs",
      "weight": 9,
      "description": "Given a grid of obstacles and open cells, compute the shortest path from top-left to bottom-right using BFS. Return -1 if unreachable.",
      "signature": "def shortest_path(grid: List[List[int]]) -> int:"
    },
    {
      "id": "grid_word_search_backtracking",
      "name": "Word Search Backtracking",
      "category": "grids",
      "topic": "backtracking",
      "weight": 7,
      "description": "Given a 2D grid of letters and a word, determine if the word exists in the grid via backtracking with visited handling.",
      "signature": "def exist(board: List[List[str]], word: str) -> bool:"
    }
  ]
}
